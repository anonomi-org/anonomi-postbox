android.useAndroidX=true
# Packaging holds the native libraries in memory, and there are four ABIs of
# them. 1g ran out of heap doing that for two variants at once. Same value as
# anonomi-android, which builds this project as a submodule.
org.gradle.jvmargs=-Xmx2g
org.gradle.dependency.verification.console=verbose
