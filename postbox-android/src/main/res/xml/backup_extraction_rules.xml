<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- docs: https://developer.android.com/guide/topics/data/autobackup#XMLSyntax -->
    <cloud-backup disableIfNoEncryptionCapabilities="true">
        <exclude
            domain="root"
            path="." />
        <!-- the previous entry should exclude things recursively, but we add more just to be sure -->
        <!-- the onion service key and the auth tokens live in getFilesDir()/db -->
        <exclude
            domain="file"
            path="." />
        <exclude
            domain="database"
            path="." />
        <exclude
            domain="sharedpref"
            path="." />
        <exclude
            domain="external"
            path="." />
    </cloud-backup>
    <!-- allowBackup=false does not reach device-to-device transfer on some
         manufacturers' devices from API 31, so it has to be excluded here too -->
    <device-transfer>
        <exclude
            domain="root"
            path="." />
        <exclude
            domain="file"
            path="." />
        <exclude
            domain="database"
            path="." />
        <exclude
            domain="sharedpref"
            path="." />
        <exclude
            domain="external"
            path="." />
    </device-transfer>
</data-extraction-rules>
